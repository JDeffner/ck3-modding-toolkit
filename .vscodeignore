.vscode/**
.vscode-test/**
.claude/**
docs/**
client/**
server/**
shared/**
test/**
scripts/**
skills/**
node_modules/**
# Ship the bundled frequency table (§C3) even though shared/** is excluded.
!shared/data/freqs.json
AGENTS.md
.github/**
dev-paths.example.json
dev-paths.json
.gitignore
.gitattributes
tsconfig.json
vitest.config.ts
pnpm-lock.yaml
pnpm-workspace.yaml
**/*.map
*.vsix
# esbuild-bundled diag/audit scripts land in dist/ — never ship them.
dist/*.cjs
# Build-time input for shared/data/dataTypes.json (bundled into dist/server.js).
wikidocs/Data_types.md
